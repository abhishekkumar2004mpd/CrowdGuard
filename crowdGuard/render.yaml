services:
  - type: web
    name: crowdctrl-backend
    env: python
    rootDir: crowdGuard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT wsgi:app
    plan: free
